/* 자바스크립트는 웹과 사용자와 동적으로 상호작용 할 수 있게 해주는 언어이다. */
console.log("java script");

function Empty(){
    if(frm.userid.value ==""){
        frm.userid.focus();
        document.getElementById("erMsg").style.display="block";
        return false;
    }
}
